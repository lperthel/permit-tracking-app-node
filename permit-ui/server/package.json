{
  "name": "angulardemo",
  "version": "0.0.0",
  "dependencies": {
    "faker": "^5.5.3",
    "json-server": "^1.0.0-beta.3"
  },
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "test": "ng test",
    "lint": "ng lint",
    "e2e": "ng e2e",
    "generate": "node ./generateData.mjs",
    "gs": "npm run generate && npm run server",
    "server": "json-server --watch ./database.json"
  }
}
